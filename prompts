rocket_engine_safety_shutdown:

Act like an experienced Agentic AI designer.

Your goal is to design a simple reflex agent system for rocket engine safety shutdown, where if the engine temperature is greater than a threshold, then trigger automatic shutdown to prevent catastrophic failure during launch.

Write a python code in Google Colab, use langgraph framework. Use open source LLM llama-3.1-8b-instant and call the LLM from Groq.

Write the code in following sequence:
1. Install necessary libraries
2. Import libraries
3. Give Groq API
4. Write the logic for the rocket engine safety shutdown
5. LLM should give decorated output
6. Show the output using Gradio; design a professional UI/UX interface. Show moving horizontal line with the name Rocket Engine Safety Shutdown by Kumampati Aerospace. Make professional product

---

network_bandwidth_optimization:

Act like an experienced Agentic AI designer.

Your goal is to design a goal-based AI agent system for optimizing network bandwidth in avionics routers.

Write a python code in Google Colab, use crewai framework. Use open source LLM llama-3.1-8b-instant and call the LLM from Groq.

Write the code in following sequence:
1. Install necessary libraries
2. Import libraries
3. Give Groq API
4. Make a decision using LLM based on current application usage of network bandwidth.
6. Show the output using Gradio; design a professional UI/UX interface.

---

pdf summarizer:

Act as an experienced AI engineer.

Your objective is to create a hybrid RAG application with langchain, which can provide a summary of the uploaded PDF and also answer questions based on the PDF. Use all open-source modules for the architecture. Use the following sequence:

1. Chunk
2. Tokenize
3. Embed (use hybrid: dense + sparse)
4. Index
5. VectorDB (use FAISS)
6. Retrieval
7. LLMs
8. Generate Response

Show output (head) for each step.

Write the code for Google Colab. Generate a professional UI using Gradio.